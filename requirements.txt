# ============================================================================
# DUET CSE Carnival 2026 - Bangladesh Highway Vehicle Detection
# Tested target: Kaggle GPU notebook (T4 x2 / P100, CUDA 12.x, Python 3.10/3.11)
# Most of these are pre-installed on Kaggle; pin only what matters.
# ============================================================================

# --- Core detector (YOLO11 / YOLOv8 / RT-DETR all live here) ---
ultralytics>=8.3.0          # YOLO11, YOLOv8, RT-DETR; mosaic/copy-paste/TTA built in

# --- Deep learning backend (Kaggle already ships a CUDA build; do not reinstall there) ---
torch>=2.1.0
torchvision>=0.16.0

# --- Augmentation (weather/blur/noise pipeline hooked into Ultralytics) ---
albumentations>=1.4.0
opencv-python-headless>=4.9.0

# --- Box fusion for ensembling (WBF / Soft-NMS) ---
ensemble-boxes>=1.0.9

# --- Data / metrics / plotting (EDA + local mAP@0.5) ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3          # stratified/group helpers, sanity checks
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0
tqdm>=4.66
pillow>=10.0

# --- Optional experiment tracking (off by default; enable in train.py) ---
# wandb>=0.16
